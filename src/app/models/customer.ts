export interface CustomerI {
    id?: number;
    first_name: string;
    last_name: string;
    email: string;
    phone?: string;
}

export interface CustomerResponseI {
    id?: number;
    first_name: string;
    last_name: string;
    email: string;
    phone?: string;
}